# Names of nodes to start
#   most will only start one node:
CELERYD_NODES="worker db_worker slow_db_worker nassl_worker"

# Absolute or relative path to the 'celery' command:
CELERY_BIN="/opt/internetnl/Internet.nl/.venv/bin/celery"

# App instance to use
# comment out this line if you don't use an app
CELERY_APP="internetnl"

# Where to chdir at start.
CELERYD_CHDIR="/opt/internetnl/Internet.nl/"

# Extra command-line arguments to the worker
#CELERYD_OPTS="--without-gossip --time-limit=300 -P eventlet -c:1 50 -c:2 3 -Q:2 db_worker -c:3 3 -Q:3 slow_db_worker -c:4 50 -Q:4 nassl_worker"
CELERYD_OPTS="--without-gossip --time-limit=300 -P gevent -c:1 50 -c:2 3 -Q:2 db_worker -c:3 3 -Q:3 slow_db_worker -c:4 50 -Q:4 nassl_worker"

# %N will be replaced with the first part of the nodename.
CELERYD_LOG_FILE="/opt/internetnl/log/celery-%N.log"
CELERYD_PID_FILE="/opt/internetnl/run/celery-%N.pid"

CELERYD_LOG_LEVEL="INFO"

# Workers should run as an unprivileged user.
#   You need to create this user manually (or you can choose
#   a user/group combination that already exists, e.g. nobody).
CELERYD_USER="internetnl"
CELERYD_GROUP="internetnl"

# If enabled pid and log directories will be created if missing,
# and owned by the userid/group configured.
CELERY_CREATE_DIRS=1


# Probably a pre 1.4 version, that might not be anywhere else...
# Names of nodes to start
# CELERYD_NODES="worker db_worker slow_db_worker"
#
# # Absolute or relative path to the 'celery' command:
# CELERY_BIN="/opt/internetnl/Internet.nl/.venv/bin/celery"
#
# # App instance to use
# CELERY_APP="internetnl"
#
# # Where to chdir at start.
# CELERYD_CHDIR="<path-to-internetnl>"
#
# # Extra command-line arguments to the worker
# CELERYD_OPTS="--without-gossip --time-limit=300 -P eventlet -c:1 250 -c:2 1 -Q:2 db_worker -c:3 3 -Q:3 slow_db_worker"
#
# # %N will be replaced with the first part of the nodename.
# CELERYD_LOG_FILE="/opt/internetnl/log/celery/%N.log"
# CELERYD_PID_FILE="/opt/internetnl/run/celery/%N.pid"
#
# # Workers should run as an unprivileged user.
# #   You need to create this user manually (or you can choose
# #   a user/group combination that already exists, e.g. nobody).
# CELERYD_USER="celery"
# CELERYD_GROUP="celery"
#
# # If enabled pid and log directories will be created if missing,
# # and owned by the userid/group configured.
# CELERY_CREATE_DIRS=1
#